{
  "name": "customdomainready",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Static site - no build required'",
    "dev": "npx serve . -p 3000",
    "lint": "echo 'No lint configured'"
  }
}
