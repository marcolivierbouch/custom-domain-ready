# Custom Domain Ready - Marketing Site

Static HTML site. No React, no build step. Served directly from `index.html`.

## Local development

```bash
pnpm dev
# or: npx serve . -p 3000
```

Opens at http://localhost:3000

## Deploy to Vercel

Point Vercel to this directory. The project uses `vercel.json` for static deployment.
